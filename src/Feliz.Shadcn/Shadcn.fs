// This file is auto-generated by the Generate.fsx script

namespace Feliz.Shadcn

[<AutoOpen>]
module Elements =
    open Fable.Core
    open Fable.Core.JsInterop
    open Feliz
    open Feliz.Shadcn.Helpers

    [<Erase>]
    type Shadcn =
        static member inline accordion (props: IReactProperty seq) = createElement(import "Accordion" "@/components/ui/accordion") props
        static member inline accordion (children: #seq<ReactElement>) = createElement(import "Accordion" "@/components/ui/accordion") [ prop.children (children :> ReactElement seq) ]
        static member inline accordionItem (props: IReactProperty seq) = createElement(import "AccordionItem" "@/components/ui/accordion") props
        static member inline accordionItem (children: #seq<ReactElement>) = createElement(import "AccordionItem" "@/components/ui/accordion") [ prop.children (children :> ReactElement seq) ]
        static member inline accordionItem (text: string) = createElement(import "AccordionItem" "@/components/ui/accordion") [ prop.text text ]
        static member inline accordionTrigger (props: IReactProperty seq) = createElement(import "AccordionTrigger" "@/components/ui/accordion") props
        static member inline accordionTrigger (children: #seq<ReactElement>) = createElement(import "AccordionTrigger" "@/components/ui/accordion") [ prop.children (children :> ReactElement seq) ]
        static member inline accordionTrigger (text: string) = createElement(import "AccordionTrigger" "@/components/ui/accordion") [ prop.text text ]
        static member inline accordionContent (props: IReactProperty seq) = createElement(import "AccordionContent" "@/components/ui/accordion") props
        static member inline accordionContent (children: #seq<ReactElement>) = createElement(import "AccordionContent" "@/components/ui/accordion") [ prop.children (children :> ReactElement seq) ]
        static member inline accordionContent (text: string) = createElement(import "AccordionContent" "@/components/ui/accordion") [ prop.text text ]

        static member inline alert (props: IReactProperty seq) = createElement(import "Alert" "@/components/ui/alert") props
        static member inline alert (children: #seq<ReactElement>) = createElement(import "Alert" "@/components/ui/alert") [ prop.children (children :> ReactElement seq) ]
        static member inline alertTitle (props: IReactProperty seq) = createElement(import "AlertTitle" "@/components/ui/alert") props
        static member inline alertTitle (children: #seq<ReactElement>) = createElement(import "AlertTitle" "@/components/ui/alert") [ prop.children (children :> ReactElement seq) ]
        static member inline alertTitle (text: string) = createElement(import "AlertTitle" "@/components/ui/alert") [ prop.text text ]
        static member inline alertDescription (props: IReactProperty seq) = createElement(import "AlertDescription" "@/components/ui/alert") props
        static member inline alertDescription (children: #seq<ReactElement>) = createElement(import "AlertDescription" "@/components/ui/alert") [ prop.children (children :> ReactElement seq) ]
        static member inline alertDescription (text: string) = createElement(import "AlertDescription" "@/components/ui/alert") [ prop.text text ]

        static member inline alertDialog (props: IReactProperty seq) = createElement(import "AlertDialog" "@/components/ui/alert-dialog") props
        static member inline alertDialog (children: #seq<ReactElement>) = createElement(import "AlertDialog" "@/components/ui/alert-dialog") [ prop.children (children :> ReactElement seq) ]
        static member inline alertDialogPortal (props: IReactProperty seq) = createElement(import "AlertDialogPortal" "@/components/ui/alert-dialog") props
        static member inline alertDialogPortal (children: #seq<ReactElement>) = createElement(import "AlertDialogPortal" "@/components/ui/alert-dialog") [ prop.children (children :> ReactElement seq) ]
        static member inline alertDialogPortal (text: string) = createElement(import "AlertDialogPortal" "@/components/ui/alert-dialog") [ prop.text text ]
        static member inline alertDialogOverlay (props: IReactProperty seq) = createElement(import "AlertDialogOverlay" "@/components/ui/alert-dialog") props
        static member inline alertDialogOverlay (children: #seq<ReactElement>) = createElement(import "AlertDialogOverlay" "@/components/ui/alert-dialog") [ prop.children (children :> ReactElement seq) ]
        static member inline alertDialogOverlay (text: string) = createElement(import "AlertDialogOverlay" "@/components/ui/alert-dialog") [ prop.text text ]
        static member inline alertDialogTrigger (props: IReactProperty seq) = createElement(import "AlertDialogTrigger" "@/components/ui/alert-dialog") props
        static member inline alertDialogTrigger (children: #seq<ReactElement>) = createElement(import "AlertDialogTrigger" "@/components/ui/alert-dialog") [ prop.children (children :> ReactElement seq) ]
        static member inline alertDialogTrigger (text: string) = createElement(import "AlertDialogTrigger" "@/components/ui/alert-dialog") [ prop.text text ]
        static member inline alertDialogContent (props: IReactProperty seq) = createElement(import "AlertDialogContent" "@/components/ui/alert-dialog") props
        static member inline alertDialogContent (children: #seq<ReactElement>) = createElement(import "AlertDialogContent" "@/components/ui/alert-dialog") [ prop.children (children :> ReactElement seq) ]
        static member inline alertDialogContent (text: string) = createElement(import "AlertDialogContent" "@/components/ui/alert-dialog") [ prop.text text ]
        static member inline alertDialogHeader (props: IReactProperty seq) = createElement(import "AlertDialogHeader" "@/components/ui/alert-dialog") props
        static member inline alertDialogHeader (children: #seq<ReactElement>) = createElement(import "AlertDialogHeader" "@/components/ui/alert-dialog") [ prop.children (children :> ReactElement seq) ]
        static member inline alertDialogHeader (text: string) = createElement(import "AlertDialogHeader" "@/components/ui/alert-dialog") [ prop.text text ]
        static member inline alertDialogFooter (props: IReactProperty seq) = createElement(import "AlertDialogFooter" "@/components/ui/alert-dialog") props
        static member inline alertDialogFooter (children: #seq<ReactElement>) = createElement(import "AlertDialogFooter" "@/components/ui/alert-dialog") [ prop.children (children :> ReactElement seq) ]
        static member inline alertDialogFooter (text: string) = createElement(import "AlertDialogFooter" "@/components/ui/alert-dialog") [ prop.text text ]
        static member inline alertDialogTitle (props: IReactProperty seq) = createElement(import "AlertDialogTitle" "@/components/ui/alert-dialog") props
        static member inline alertDialogTitle (children: #seq<ReactElement>) = createElement(import "AlertDialogTitle" "@/components/ui/alert-dialog") [ prop.children (children :> ReactElement seq) ]
        static member inline alertDialogTitle (text: string) = createElement(import "AlertDialogTitle" "@/components/ui/alert-dialog") [ prop.text text ]
        static member inline alertDialogDescription (props: IReactProperty seq) = createElement(import "AlertDialogDescription" "@/components/ui/alert-dialog") props
        static member inline alertDialogDescription (children: #seq<ReactElement>) = createElement(import "AlertDialogDescription" "@/components/ui/alert-dialog") [ prop.children (children :> ReactElement seq) ]
        static member inline alertDialogDescription (text: string) = createElement(import "AlertDialogDescription" "@/components/ui/alert-dialog") [ prop.text text ]
        static member inline alertDialogAction (props: IReactProperty seq) = createElement(import "AlertDialogAction" "@/components/ui/alert-dialog") props
        static member inline alertDialogAction (children: #seq<ReactElement>) = createElement(import "AlertDialogAction" "@/components/ui/alert-dialog") [ prop.children (children :> ReactElement seq) ]
        static member inline alertDialogAction (text: string) = createElement(import "AlertDialogAction" "@/components/ui/alert-dialog") [ prop.text text ]
        static member inline alertDialogCancel (props: IReactProperty seq) = createElement(import "AlertDialogCancel" "@/components/ui/alert-dialog") props
        static member inline alertDialogCancel (children: #seq<ReactElement>) = createElement(import "AlertDialogCancel" "@/components/ui/alert-dialog") [ prop.children (children :> ReactElement seq) ]
        static member inline alertDialogCancel (text: string) = createElement(import "AlertDialogCancel" "@/components/ui/alert-dialog") [ prop.text text ]

        static member inline aspectRatio (props: IReactProperty seq) = createElement(import "AspectRatio" "@/components/ui/aspect-ratio") props
        static member inline aspectRatio (children: #seq<ReactElement>) = createElement(import "AspectRatio" "@/components/ui/aspect-ratio") [ prop.children (children :> ReactElement seq) ]

        static member inline avatar (props: IReactProperty seq) = createElement(import "Avatar" "@/components/ui/avatar") props
        static member inline avatar (children: #seq<ReactElement>) = createElement(import "Avatar" "@/components/ui/avatar") [ prop.children (children :> ReactElement seq) ]
        static member inline avatarImage (props: IReactProperty seq) = createElement(import "AvatarImage" "@/components/ui/avatar") props
        static member inline avatarImage (children: #seq<ReactElement>) = createElement(import "AvatarImage" "@/components/ui/avatar") [ prop.children (children :> ReactElement seq) ]
        static member inline avatarImage (text: string) = createElement(import "AvatarImage" "@/components/ui/avatar") [ prop.text text ]
        static member inline avatarFallback (props: IReactProperty seq) = createElement(import "AvatarFallback" "@/components/ui/avatar") props
        static member inline avatarFallback (children: #seq<ReactElement>) = createElement(import "AvatarFallback" "@/components/ui/avatar") [ prop.children (children :> ReactElement seq) ]
        static member inline avatarFallback (text: string) = createElement(import "AvatarFallback" "@/components/ui/avatar") [ prop.text text ]

        static member inline badge (props: IReactProperty seq) = createElement(import "Badge" "@/components/ui/badge") props
        static member inline badge (children: #seq<ReactElement>) = createElement(import "Badge" "@/components/ui/badge") [ prop.children (children :> ReactElement seq) ]

        static member inline breadcrumb (props: IReactProperty seq) = createElement(import "Breadcrumb" "@/components/ui/breadcrumb") props
        static member inline breadcrumb (children: #seq<ReactElement>) = createElement(import "Breadcrumb" "@/components/ui/breadcrumb") [ prop.children (children :> ReactElement seq) ]
        static member inline breadcrumbList (props: IReactProperty seq) = createElement(import "BreadcrumbList" "@/components/ui/breadcrumb") props
        static member inline breadcrumbList (children: #seq<ReactElement>) = createElement(import "BreadcrumbList" "@/components/ui/breadcrumb") [ prop.children (children :> ReactElement seq) ]
        static member inline breadcrumbList (text: string) = createElement(import "BreadcrumbList" "@/components/ui/breadcrumb") [ prop.text text ]
        static member inline breadcrumbItem (props: IReactProperty seq) = createElement(import "BreadcrumbItem" "@/components/ui/breadcrumb") props
        static member inline breadcrumbItem (children: #seq<ReactElement>) = createElement(import "BreadcrumbItem" "@/components/ui/breadcrumb") [ prop.children (children :> ReactElement seq) ]
        static member inline breadcrumbItem (text: string) = createElement(import "BreadcrumbItem" "@/components/ui/breadcrumb") [ prop.text text ]
        static member inline breadcrumbLink (props: IReactProperty seq) = createElement(import "BreadcrumbLink" "@/components/ui/breadcrumb") props
        static member inline breadcrumbLink (children: #seq<ReactElement>) = createElement(import "BreadcrumbLink" "@/components/ui/breadcrumb") [ prop.children (children :> ReactElement seq) ]
        static member inline breadcrumbLink (text: string) = createElement(import "BreadcrumbLink" "@/components/ui/breadcrumb") [ prop.text text ]
        static member inline breadcrumbPage (props: IReactProperty seq) = createElement(import "BreadcrumbPage" "@/components/ui/breadcrumb") props
        static member inline breadcrumbPage (children: #seq<ReactElement>) = createElement(import "BreadcrumbPage" "@/components/ui/breadcrumb") [ prop.children (children :> ReactElement seq) ]
        static member inline breadcrumbPage (text: string) = createElement(import "BreadcrumbPage" "@/components/ui/breadcrumb") [ prop.text text ]
        static member inline breadcrumbSeparator (props: IReactProperty seq) = createElement(import "BreadcrumbSeparator" "@/components/ui/breadcrumb") props
        static member inline breadcrumbSeparator (children: #seq<ReactElement>) = createElement(import "BreadcrumbSeparator" "@/components/ui/breadcrumb") [ prop.children (children :> ReactElement seq) ]
        static member inline breadcrumbSeparator (text: string) = createElement(import "BreadcrumbSeparator" "@/components/ui/breadcrumb") [ prop.text text ]
        static member inline breadcrumbEllipsis (props: IReactProperty seq) = createElement(import "BreadcrumbEllipsis" "@/components/ui/breadcrumb") props
        static member inline breadcrumbEllipsis (children: #seq<ReactElement>) = createElement(import "BreadcrumbEllipsis" "@/components/ui/breadcrumb") [ prop.children (children :> ReactElement seq) ]
        static member inline breadcrumbEllipsis (text: string) = createElement(import "BreadcrumbEllipsis" "@/components/ui/breadcrumb") [ prop.text text ]

        static member inline button (props: IReactProperty seq) = createElement(import "Button" "@/components/ui/button") props
        static member inline button (children: #seq<ReactElement>) = createElement(import "Button" "@/components/ui/button") [ prop.children (children :> ReactElement seq) ]

        static member inline calendar (props: IReactProperty seq) = createElement(import "Calendar" "@/components/ui/calendar") props
        static member inline calendar (children: #seq<ReactElement>) = createElement(import "Calendar" "@/components/ui/calendar") [ prop.children (children :> ReactElement seq) ]

        static member inline card (props: IReactProperty seq) = createElement(import "Card" "@/components/ui/card") props
        static member inline card (children: #seq<ReactElement>) = createElement(import "Card" "@/components/ui/card") [ prop.children (children :> ReactElement seq) ]
        static member inline cardHeader (props: IReactProperty seq) = createElement(import "CardHeader" "@/components/ui/card") props
        static member inline cardHeader (children: #seq<ReactElement>) = createElement(import "CardHeader" "@/components/ui/card") [ prop.children (children :> ReactElement seq) ]
        static member inline cardHeader (text: string) = createElement(import "CardHeader" "@/components/ui/card") [ prop.text text ]
        static member inline cardFooter (props: IReactProperty seq) = createElement(import "CardFooter" "@/components/ui/card") props
        static member inline cardFooter (children: #seq<ReactElement>) = createElement(import "CardFooter" "@/components/ui/card") [ prop.children (children :> ReactElement seq) ]
        static member inline cardFooter (text: string) = createElement(import "CardFooter" "@/components/ui/card") [ prop.text text ]
        static member inline cardTitle (props: IReactProperty seq) = createElement(import "CardTitle" "@/components/ui/card") props
        static member inline cardTitle (children: #seq<ReactElement>) = createElement(import "CardTitle" "@/components/ui/card") [ prop.children (children :> ReactElement seq) ]
        static member inline cardTitle (text: string) = createElement(import "CardTitle" "@/components/ui/card") [ prop.text text ]
        static member inline cardDescription (props: IReactProperty seq) = createElement(import "CardDescription" "@/components/ui/card") props
        static member inline cardDescription (children: #seq<ReactElement>) = createElement(import "CardDescription" "@/components/ui/card") [ prop.children (children :> ReactElement seq) ]
        static member inline cardDescription (text: string) = createElement(import "CardDescription" "@/components/ui/card") [ prop.text text ]
        static member inline cardContent (props: IReactProperty seq) = createElement(import "CardContent" "@/components/ui/card") props
        static member inline cardContent (children: #seq<ReactElement>) = createElement(import "CardContent" "@/components/ui/card") [ prop.children (children :> ReactElement seq) ]
        static member inline cardContent (text: string) = createElement(import "CardContent" "@/components/ui/card") [ prop.text text ]

        static member inline carousel (props: IReactProperty seq) = createElement(import "Carousel" "@/components/ui/carousel") props
        static member inline carousel (children: #seq<ReactElement>) = createElement(import "Carousel" "@/components/ui/carousel") [ prop.children (children :> ReactElement seq) ]

        static member inline chartContainer (props: IReactProperty seq) = createElement(import "ChartContainer" "@/components/ui/chart") props
        static member inline chartContainer (children: #seq<ReactElement>) = createElement(import "ChartContainer" "@/components/ui/chart") [ prop.children (children :> ReactElement seq) ]

        static member inline checkbox (props: IReactProperty seq) = createElement(import "Checkbox" "@/components/ui/checkbox") props
        static member inline checkbox (children: #seq<ReactElement>) = createElement(import "Checkbox" "@/components/ui/checkbox") [ prop.children (children :> ReactElement seq) ]

        static member inline collapsible (props: IReactProperty seq) = createElement(import "Collapsible" "@/components/ui/collapsible") props
        static member inline collapsible (children: #seq<ReactElement>) = createElement(import "Collapsible" "@/components/ui/collapsible") [ prop.children (children :> ReactElement seq) ]
        static member inline collapsibleTrigger (props: IReactProperty seq) = createElement(import "CollapsibleTrigger" "@/components/ui/collapsible") props
        static member inline collapsibleTrigger (children: #seq<ReactElement>) = createElement(import "CollapsibleTrigger" "@/components/ui/collapsible") [ prop.children (children :> ReactElement seq) ]
        static member inline collapsibleTrigger (text: string) = createElement(import "CollapsibleTrigger" "@/components/ui/collapsible") [ prop.text text ]
        static member inline collapsibleContent (props: IReactProperty seq) = createElement(import "CollapsibleContent" "@/components/ui/collapsible") props
        static member inline collapsibleContent (children: #seq<ReactElement>) = createElement(import "CollapsibleContent" "@/components/ui/collapsible") [ prop.children (children :> ReactElement seq) ]
        static member inline collapsibleContent (text: string) = createElement(import "CollapsibleContent" "@/components/ui/collapsible") [ prop.text text ]

        static member inline command (props: IReactProperty seq) = createElement(import "Command" "@/components/ui/command") props
        static member inline command (children: #seq<ReactElement>) = createElement(import "Command" "@/components/ui/command") [ prop.children (children :> ReactElement seq) ]
        static member inline commandDialog (props: IReactProperty seq) = createElement(import "CommandDialog" "@/components/ui/command") props
        static member inline commandDialog (children: #seq<ReactElement>) = createElement(import "CommandDialog" "@/components/ui/command") [ prop.children (children :> ReactElement seq) ]
        static member inline commandDialog (text: string) = createElement(import "CommandDialog" "@/components/ui/command") [ prop.text text ]
        static member inline commandInput (props: IReactProperty seq) = createElement(import "CommandInput" "@/components/ui/command") props
        static member inline commandInput (children: #seq<ReactElement>) = createElement(import "CommandInput" "@/components/ui/command") [ prop.children (children :> ReactElement seq) ]
        static member inline commandInput (text: string) = createElement(import "CommandInput" "@/components/ui/command") [ prop.text text ]
        static member inline commandList (props: IReactProperty seq) = createElement(import "CommandList" "@/components/ui/command") props
        static member inline commandList (children: #seq<ReactElement>) = createElement(import "CommandList" "@/components/ui/command") [ prop.children (children :> ReactElement seq) ]
        static member inline commandList (text: string) = createElement(import "CommandList" "@/components/ui/command") [ prop.text text ]
        static member inline commandEmpty (props: IReactProperty seq) = createElement(import "CommandEmpty" "@/components/ui/command") props
        static member inline commandEmpty (children: #seq<ReactElement>) = createElement(import "CommandEmpty" "@/components/ui/command") [ prop.children (children :> ReactElement seq) ]
        static member inline commandEmpty (text: string) = createElement(import "CommandEmpty" "@/components/ui/command") [ prop.text text ]
        static member inline commandGroup (props: IReactProperty seq) = createElement(import "CommandGroup" "@/components/ui/command") props
        static member inline commandGroup (children: #seq<ReactElement>) = createElement(import "CommandGroup" "@/components/ui/command") [ prop.children (children :> ReactElement seq) ]
        static member inline commandGroup (text: string) = createElement(import "CommandGroup" "@/components/ui/command") [ prop.text text ]
        static member inline commandItem (props: IReactProperty seq) = createElement(import "CommandItem" "@/components/ui/command") props
        static member inline commandItem (children: #seq<ReactElement>) = createElement(import "CommandItem" "@/components/ui/command") [ prop.children (children :> ReactElement seq) ]
        static member inline commandItem (text: string) = createElement(import "CommandItem" "@/components/ui/command") [ prop.text text ]
        static member inline commandShortcut (props: IReactProperty seq) = createElement(import "CommandShortcut" "@/components/ui/command") props
        static member inline commandShortcut (children: #seq<ReactElement>) = createElement(import "CommandShortcut" "@/components/ui/command") [ prop.children (children :> ReactElement seq) ]
        static member inline commandShortcut (text: string) = createElement(import "CommandShortcut" "@/components/ui/command") [ prop.text text ]
        static member inline commandSeparator (props: IReactProperty seq) = createElement(import "CommandSeparator" "@/components/ui/command") props
        static member inline commandSeparator (children: #seq<ReactElement>) = createElement(import "CommandSeparator" "@/components/ui/command") [ prop.children (children :> ReactElement seq) ]
        static member inline commandSeparator (text: string) = createElement(import "CommandSeparator" "@/components/ui/command") [ prop.text text ]

        static member inline contextMenu (props: IReactProperty seq) = createElement(import "ContextMenu" "@/components/ui/context-menu") props
        static member inline contextMenu (children: #seq<ReactElement>) = createElement(import "ContextMenu" "@/components/ui/context-menu") [ prop.children (children :> ReactElement seq) ]
        static member inline contextMenuTrigger (props: IReactProperty seq) = createElement(import "ContextMenuTrigger" "@/components/ui/context-menu") props
        static member inline contextMenuTrigger (children: #seq<ReactElement>) = createElement(import "ContextMenuTrigger" "@/components/ui/context-menu") [ prop.children (children :> ReactElement seq) ]
        static member inline contextMenuTrigger (text: string) = createElement(import "ContextMenuTrigger" "@/components/ui/context-menu") [ prop.text text ]
        static member inline contextMenuContent (props: IReactProperty seq) = createElement(import "ContextMenuContent" "@/components/ui/context-menu") props
        static member inline contextMenuContent (children: #seq<ReactElement>) = createElement(import "ContextMenuContent" "@/components/ui/context-menu") [ prop.children (children :> ReactElement seq) ]
        static member inline contextMenuContent (text: string) = createElement(import "ContextMenuContent" "@/components/ui/context-menu") [ prop.text text ]
        static member inline contextMenuItem (props: IReactProperty seq) = createElement(import "ContextMenuItem" "@/components/ui/context-menu") props
        static member inline contextMenuItem (children: #seq<ReactElement>) = createElement(import "ContextMenuItem" "@/components/ui/context-menu") [ prop.children (children :> ReactElement seq) ]
        static member inline contextMenuItem (text: string) = createElement(import "ContextMenuItem" "@/components/ui/context-menu") [ prop.text text ]
        static member inline contextMenuCheckboxItem (props: IReactProperty seq) = createElement(import "ContextMenuCheckboxItem" "@/components/ui/context-menu") props
        static member inline contextMenuCheckboxItem (children: #seq<ReactElement>) = createElement(import "ContextMenuCheckboxItem" "@/components/ui/context-menu") [ prop.children (children :> ReactElement seq) ]
        static member inline contextMenuCheckboxItem (text: string) = createElement(import "ContextMenuCheckboxItem" "@/components/ui/context-menu") [ prop.text text ]
        static member inline contextMenuRadioItem (props: IReactProperty seq) = createElement(import "ContextMenuRadioItem" "@/components/ui/context-menu") props
        static member inline contextMenuRadioItem (children: #seq<ReactElement>) = createElement(import "ContextMenuRadioItem" "@/components/ui/context-menu") [ prop.children (children :> ReactElement seq) ]
        static member inline contextMenuRadioItem (text: string) = createElement(import "ContextMenuRadioItem" "@/components/ui/context-menu") [ prop.text text ]
        static member inline contextMenuLabel (props: IReactProperty seq) = createElement(import "ContextMenuLabel" "@/components/ui/context-menu") props
        static member inline contextMenuLabel (children: #seq<ReactElement>) = createElement(import "ContextMenuLabel" "@/components/ui/context-menu") [ prop.children (children :> ReactElement seq) ]
        static member inline contextMenuLabel (text: string) = createElement(import "ContextMenuLabel" "@/components/ui/context-menu") [ prop.text text ]
        static member inline contextMenuSeparator (props: IReactProperty seq) = createElement(import "ContextMenuSeparator" "@/components/ui/context-menu") props
        static member inline contextMenuSeparator (children: #seq<ReactElement>) = createElement(import "ContextMenuSeparator" "@/components/ui/context-menu") [ prop.children (children :> ReactElement seq) ]
        static member inline contextMenuSeparator (text: string) = createElement(import "ContextMenuSeparator" "@/components/ui/context-menu") [ prop.text text ]
        static member inline contextMenuShortcut (props: IReactProperty seq) = createElement(import "ContextMenuShortcut" "@/components/ui/context-menu") props
        static member inline contextMenuShortcut (children: #seq<ReactElement>) = createElement(import "ContextMenuShortcut" "@/components/ui/context-menu") [ prop.children (children :> ReactElement seq) ]
        static member inline contextMenuShortcut (text: string) = createElement(import "ContextMenuShortcut" "@/components/ui/context-menu") [ prop.text text ]
        static member inline contextMenuGroup (props: IReactProperty seq) = createElement(import "ContextMenuGroup" "@/components/ui/context-menu") props
        static member inline contextMenuGroup (children: #seq<ReactElement>) = createElement(import "ContextMenuGroup" "@/components/ui/context-menu") [ prop.children (children :> ReactElement seq) ]
        static member inline contextMenuGroup (text: string) = createElement(import "ContextMenuGroup" "@/components/ui/context-menu") [ prop.text text ]
        static member inline contextMenuPortal (props: IReactProperty seq) = createElement(import "ContextMenuPortal" "@/components/ui/context-menu") props
        static member inline contextMenuPortal (children: #seq<ReactElement>) = createElement(import "ContextMenuPortal" "@/components/ui/context-menu") [ prop.children (children :> ReactElement seq) ]
        static member inline contextMenuPortal (text: string) = createElement(import "ContextMenuPortal" "@/components/ui/context-menu") [ prop.text text ]
        static member inline contextMenuSub (props: IReactProperty seq) = createElement(import "ContextMenuSub" "@/components/ui/context-menu") props
        static member inline contextMenuSub (children: #seq<ReactElement>) = createElement(import "ContextMenuSub" "@/components/ui/context-menu") [ prop.children (children :> ReactElement seq) ]
        static member inline contextMenuSub (text: string) = createElement(import "ContextMenuSub" "@/components/ui/context-menu") [ prop.text text ]
        static member inline contextMenuSubContent (props: IReactProperty seq) = createElement(import "ContextMenuSubContent" "@/components/ui/context-menu") props
        static member inline contextMenuSubContent (children: #seq<ReactElement>) = createElement(import "ContextMenuSubContent" "@/components/ui/context-menu") [ prop.children (children :> ReactElement seq) ]
        static member inline contextMenuSubContent (text: string) = createElement(import "ContextMenuSubContent" "@/components/ui/context-menu") [ prop.text text ]
        static member inline contextMenuSubTrigger (props: IReactProperty seq) = createElement(import "ContextMenuSubTrigger" "@/components/ui/context-menu") props
        static member inline contextMenuSubTrigger (children: #seq<ReactElement>) = createElement(import "ContextMenuSubTrigger" "@/components/ui/context-menu") [ prop.children (children :> ReactElement seq) ]
        static member inline contextMenuSubTrigger (text: string) = createElement(import "ContextMenuSubTrigger" "@/components/ui/context-menu") [ prop.text text ]
        static member inline contextMenuRadioGroup (props: IReactProperty seq) = createElement(import "ContextMenuRadioGroup" "@/components/ui/context-menu") props
        static member inline contextMenuRadioGroup (children: #seq<ReactElement>) = createElement(import "ContextMenuRadioGroup" "@/components/ui/context-menu") [ prop.children (children :> ReactElement seq) ]
        static member inline contextMenuRadioGroup (text: string) = createElement(import "ContextMenuRadioGroup" "@/components/ui/context-menu") [ prop.text text ]

        static member inline dialog (props: IReactProperty seq) = createElement(import "Dialog" "@/components/ui/dialog") props
        static member inline dialog (children: #seq<ReactElement>) = createElement(import "Dialog" "@/components/ui/dialog") [ prop.children (children :> ReactElement seq) ]
        static member inline dialogClose (props: IReactProperty seq) = createElement(import "DialogClose" "@/components/ui/dialog") props
        static member inline dialogClose (children: #seq<ReactElement>) = createElement(import "DialogClose" "@/components/ui/dialog") [ prop.children (children :> ReactElement seq) ]
        static member inline dialogClose (text: string) = createElement(import "DialogClose" "@/components/ui/dialog") [ prop.text text ]
        static member inline dialogContent (props: IReactProperty seq) = createElement(import "DialogContent" "@/components/ui/dialog") props
        static member inline dialogContent (children: #seq<ReactElement>) = createElement(import "DialogContent" "@/components/ui/dialog") [ prop.children (children :> ReactElement seq) ]
        static member inline dialogContent (text: string) = createElement(import "DialogContent" "@/components/ui/dialog") [ prop.text text ]
        static member inline dialogDescription (props: IReactProperty seq) = createElement(import "DialogDescription" "@/components/ui/dialog") props
        static member inline dialogDescription (children: #seq<ReactElement>) = createElement(import "DialogDescription" "@/components/ui/dialog") [ prop.children (children :> ReactElement seq) ]
        static member inline dialogDescription (text: string) = createElement(import "DialogDescription" "@/components/ui/dialog") [ prop.text text ]
        static member inline dialogFooter (props: IReactProperty seq) = createElement(import "DialogFooter" "@/components/ui/dialog") props
        static member inline dialogFooter (children: #seq<ReactElement>) = createElement(import "DialogFooter" "@/components/ui/dialog") [ prop.children (children :> ReactElement seq) ]
        static member inline dialogFooter (text: string) = createElement(import "DialogFooter" "@/components/ui/dialog") [ prop.text text ]
        static member inline dialogHeader (props: IReactProperty seq) = createElement(import "DialogHeader" "@/components/ui/dialog") props
        static member inline dialogHeader (children: #seq<ReactElement>) = createElement(import "DialogHeader" "@/components/ui/dialog") [ prop.children (children :> ReactElement seq) ]
        static member inline dialogHeader (text: string) = createElement(import "DialogHeader" "@/components/ui/dialog") [ prop.text text ]
        static member inline dialogOverlay (props: IReactProperty seq) = createElement(import "DialogOverlay" "@/components/ui/dialog") props
        static member inline dialogOverlay (children: #seq<ReactElement>) = createElement(import "DialogOverlay" "@/components/ui/dialog") [ prop.children (children :> ReactElement seq) ]
        static member inline dialogOverlay (text: string) = createElement(import "DialogOverlay" "@/components/ui/dialog") [ prop.text text ]
        static member inline dialogPortal (props: IReactProperty seq) = createElement(import "DialogPortal" "@/components/ui/dialog") props
        static member inline dialogPortal (children: #seq<ReactElement>) = createElement(import "DialogPortal" "@/components/ui/dialog") [ prop.children (children :> ReactElement seq) ]
        static member inline dialogPortal (text: string) = createElement(import "DialogPortal" "@/components/ui/dialog") [ prop.text text ]
        static member inline dialogTitle (props: IReactProperty seq) = createElement(import "DialogTitle" "@/components/ui/dialog") props
        static member inline dialogTitle (children: #seq<ReactElement>) = createElement(import "DialogTitle" "@/components/ui/dialog") [ prop.children (children :> ReactElement seq) ]
        static member inline dialogTitle (text: string) = createElement(import "DialogTitle" "@/components/ui/dialog") [ prop.text text ]
        static member inline dialogTrigger (props: IReactProperty seq) = createElement(import "DialogTrigger" "@/components/ui/dialog") props
        static member inline dialogTrigger (children: #seq<ReactElement>) = createElement(import "DialogTrigger" "@/components/ui/dialog") [ prop.children (children :> ReactElement seq) ]
        static member inline dialogTrigger (text: string) = createElement(import "DialogTrigger" "@/components/ui/dialog") [ prop.text text ]

        static member inline drawer (props: IReactProperty seq) = createElement(import "Drawer" "@/components/ui/drawer") props
        static member inline drawer (children: #seq<ReactElement>) = createElement(import "Drawer" "@/components/ui/drawer") [ prop.children (children :> ReactElement seq) ]
        static member inline drawerPortal (props: IReactProperty seq) = createElement(import "DrawerPortal" "@/components/ui/drawer") props
        static member inline drawerPortal (children: #seq<ReactElement>) = createElement(import "DrawerPortal" "@/components/ui/drawer") [ prop.children (children :> ReactElement seq) ]
        static member inline drawerPortal (text: string) = createElement(import "DrawerPortal" "@/components/ui/drawer") [ prop.text text ]
        static member inline drawerOverlay (props: IReactProperty seq) = createElement(import "DrawerOverlay" "@/components/ui/drawer") props
        static member inline drawerOverlay (children: #seq<ReactElement>) = createElement(import "DrawerOverlay" "@/components/ui/drawer") [ prop.children (children :> ReactElement seq) ]
        static member inline drawerOverlay (text: string) = createElement(import "DrawerOverlay" "@/components/ui/drawer") [ prop.text text ]
        static member inline drawerTrigger (props: IReactProperty seq) = createElement(import "DrawerTrigger" "@/components/ui/drawer") props
        static member inline drawerTrigger (children: #seq<ReactElement>) = createElement(import "DrawerTrigger" "@/components/ui/drawer") [ prop.children (children :> ReactElement seq) ]
        static member inline drawerTrigger (text: string) = createElement(import "DrawerTrigger" "@/components/ui/drawer") [ prop.text text ]
        static member inline drawerClose (props: IReactProperty seq) = createElement(import "DrawerClose" "@/components/ui/drawer") props
        static member inline drawerClose (children: #seq<ReactElement>) = createElement(import "DrawerClose" "@/components/ui/drawer") [ prop.children (children :> ReactElement seq) ]
        static member inline drawerClose (text: string) = createElement(import "DrawerClose" "@/components/ui/drawer") [ prop.text text ]
        static member inline drawerContent (props: IReactProperty seq) = createElement(import "DrawerContent" "@/components/ui/drawer") props
        static member inline drawerContent (children: #seq<ReactElement>) = createElement(import "DrawerContent" "@/components/ui/drawer") [ prop.children (children :> ReactElement seq) ]
        static member inline drawerContent (text: string) = createElement(import "DrawerContent" "@/components/ui/drawer") [ prop.text text ]
        static member inline drawerHeader (props: IReactProperty seq) = createElement(import "DrawerHeader" "@/components/ui/drawer") props
        static member inline drawerHeader (children: #seq<ReactElement>) = createElement(import "DrawerHeader" "@/components/ui/drawer") [ prop.children (children :> ReactElement seq) ]
        static member inline drawerHeader (text: string) = createElement(import "DrawerHeader" "@/components/ui/drawer") [ prop.text text ]
        static member inline drawerFooter (props: IReactProperty seq) = createElement(import "DrawerFooter" "@/components/ui/drawer") props
        static member inline drawerFooter (children: #seq<ReactElement>) = createElement(import "DrawerFooter" "@/components/ui/drawer") [ prop.children (children :> ReactElement seq) ]
        static member inline drawerFooter (text: string) = createElement(import "DrawerFooter" "@/components/ui/drawer") [ prop.text text ]
        static member inline drawerTitle (props: IReactProperty seq) = createElement(import "DrawerTitle" "@/components/ui/drawer") props
        static member inline drawerTitle (children: #seq<ReactElement>) = createElement(import "DrawerTitle" "@/components/ui/drawer") [ prop.children (children :> ReactElement seq) ]
        static member inline drawerTitle (text: string) = createElement(import "DrawerTitle" "@/components/ui/drawer") [ prop.text text ]
        static member inline drawerDescription (props: IReactProperty seq) = createElement(import "DrawerDescription" "@/components/ui/drawer") props
        static member inline drawerDescription (children: #seq<ReactElement>) = createElement(import "DrawerDescription" "@/components/ui/drawer") [ prop.children (children :> ReactElement seq) ]
        static member inline drawerDescription (text: string) = createElement(import "DrawerDescription" "@/components/ui/drawer") [ prop.text text ]

        static member inline dropdownMenu (props: IReactProperty seq) = createElement(import "DropdownMenu" "@/components/ui/dropdown-menu") props
        static member inline dropdownMenu (children: #seq<ReactElement>) = createElement(import "DropdownMenu" "@/components/ui/dropdown-menu") [ prop.children (children :> ReactElement seq) ]
        static member inline dropdownMenuPortal (props: IReactProperty seq) = createElement(import "DropdownMenuPortal" "@/components/ui/dropdown-menu") props
        static member inline dropdownMenuPortal (children: #seq<ReactElement>) = createElement(import "DropdownMenuPortal" "@/components/ui/dropdown-menu") [ prop.children (children :> ReactElement seq) ]
        static member inline dropdownMenuPortal (text: string) = createElement(import "DropdownMenuPortal" "@/components/ui/dropdown-menu") [ prop.text text ]
        static member inline dropdownMenuTrigger (props: IReactProperty seq) = createElement(import "DropdownMenuTrigger" "@/components/ui/dropdown-menu") props
        static member inline dropdownMenuTrigger (children: #seq<ReactElement>) = createElement(import "DropdownMenuTrigger" "@/components/ui/dropdown-menu") [ prop.children (children :> ReactElement seq) ]
        static member inline dropdownMenuTrigger (text: string) = createElement(import "DropdownMenuTrigger" "@/components/ui/dropdown-menu") [ prop.text text ]
        static member inline dropdownMenuContent (props: IReactProperty seq) = createElement(import "DropdownMenuContent" "@/components/ui/dropdown-menu") props
        static member inline dropdownMenuContent (children: #seq<ReactElement>) = createElement(import "DropdownMenuContent" "@/components/ui/dropdown-menu") [ prop.children (children :> ReactElement seq) ]
        static member inline dropdownMenuContent (text: string) = createElement(import "DropdownMenuContent" "@/components/ui/dropdown-menu") [ prop.text text ]
        static member inline dropdownMenuGroup (props: IReactProperty seq) = createElement(import "DropdownMenuGroup" "@/components/ui/dropdown-menu") props
        static member inline dropdownMenuGroup (children: #seq<ReactElement>) = createElement(import "DropdownMenuGroup" "@/components/ui/dropdown-menu") [ prop.children (children :> ReactElement seq) ]
        static member inline dropdownMenuGroup (text: string) = createElement(import "DropdownMenuGroup" "@/components/ui/dropdown-menu") [ prop.text text ]
        static member inline dropdownMenuLabel (props: IReactProperty seq) = createElement(import "DropdownMenuLabel" "@/components/ui/dropdown-menu") props
        static member inline dropdownMenuLabel (children: #seq<ReactElement>) = createElement(import "DropdownMenuLabel" "@/components/ui/dropdown-menu") [ prop.children (children :> ReactElement seq) ]
        static member inline dropdownMenuLabel (text: string) = createElement(import "DropdownMenuLabel" "@/components/ui/dropdown-menu") [ prop.text text ]
        static member inline dropdownMenuItem (props: IReactProperty seq) = createElement(import "DropdownMenuItem" "@/components/ui/dropdown-menu") props
        static member inline dropdownMenuItem (children: #seq<ReactElement>) = createElement(import "DropdownMenuItem" "@/components/ui/dropdown-menu") [ prop.children (children :> ReactElement seq) ]
        static member inline dropdownMenuItem (text: string) = createElement(import "DropdownMenuItem" "@/components/ui/dropdown-menu") [ prop.text text ]
        static member inline dropdownMenuCheckboxItem (props: IReactProperty seq) = createElement(import "DropdownMenuCheckboxItem" "@/components/ui/dropdown-menu") props
        static member inline dropdownMenuCheckboxItem (children: #seq<ReactElement>) = createElement(import "DropdownMenuCheckboxItem" "@/components/ui/dropdown-menu") [ prop.children (children :> ReactElement seq) ]
        static member inline dropdownMenuCheckboxItem (text: string) = createElement(import "DropdownMenuCheckboxItem" "@/components/ui/dropdown-menu") [ prop.text text ]
        static member inline dropdownMenuRadioGroup (props: IReactProperty seq) = createElement(import "DropdownMenuRadioGroup" "@/components/ui/dropdown-menu") props
        static member inline dropdownMenuRadioGroup (children: #seq<ReactElement>) = createElement(import "DropdownMenuRadioGroup" "@/components/ui/dropdown-menu") [ prop.children (children :> ReactElement seq) ]
        static member inline dropdownMenuRadioGroup (text: string) = createElement(import "DropdownMenuRadioGroup" "@/components/ui/dropdown-menu") [ prop.text text ]
        static member inline dropdownMenuRadioItem (props: IReactProperty seq) = createElement(import "DropdownMenuRadioItem" "@/components/ui/dropdown-menu") props
        static member inline dropdownMenuRadioItem (children: #seq<ReactElement>) = createElement(import "DropdownMenuRadioItem" "@/components/ui/dropdown-menu") [ prop.children (children :> ReactElement seq) ]
        static member inline dropdownMenuRadioItem (text: string) = createElement(import "DropdownMenuRadioItem" "@/components/ui/dropdown-menu") [ prop.text text ]
        static member inline dropdownMenuSeparator (props: IReactProperty seq) = createElement(import "DropdownMenuSeparator" "@/components/ui/dropdown-menu") props
        static member inline dropdownMenuSeparator (children: #seq<ReactElement>) = createElement(import "DropdownMenuSeparator" "@/components/ui/dropdown-menu") [ prop.children (children :> ReactElement seq) ]
        static member inline dropdownMenuSeparator (text: string) = createElement(import "DropdownMenuSeparator" "@/components/ui/dropdown-menu") [ prop.text text ]
        static member inline dropdownMenuShortcut (props: IReactProperty seq) = createElement(import "DropdownMenuShortcut" "@/components/ui/dropdown-menu") props
        static member inline dropdownMenuShortcut (children: #seq<ReactElement>) = createElement(import "DropdownMenuShortcut" "@/components/ui/dropdown-menu") [ prop.children (children :> ReactElement seq) ]
        static member inline dropdownMenuShortcut (text: string) = createElement(import "DropdownMenuShortcut" "@/components/ui/dropdown-menu") [ prop.text text ]
        static member inline dropdownMenuSub (props: IReactProperty seq) = createElement(import "DropdownMenuSub" "@/components/ui/dropdown-menu") props
        static member inline dropdownMenuSub (children: #seq<ReactElement>) = createElement(import "DropdownMenuSub" "@/components/ui/dropdown-menu") [ prop.children (children :> ReactElement seq) ]
        static member inline dropdownMenuSub (text: string) = createElement(import "DropdownMenuSub" "@/components/ui/dropdown-menu") [ prop.text text ]
        static member inline dropdownMenuSubTrigger (props: IReactProperty seq) = createElement(import "DropdownMenuSubTrigger" "@/components/ui/dropdown-menu") props
        static member inline dropdownMenuSubTrigger (children: #seq<ReactElement>) = createElement(import "DropdownMenuSubTrigger" "@/components/ui/dropdown-menu") [ prop.children (children :> ReactElement seq) ]
        static member inline dropdownMenuSubTrigger (text: string) = createElement(import "DropdownMenuSubTrigger" "@/components/ui/dropdown-menu") [ prop.text text ]
        static member inline dropdownMenuSubContent (props: IReactProperty seq) = createElement(import "DropdownMenuSubContent" "@/components/ui/dropdown-menu") props
        static member inline dropdownMenuSubContent (children: #seq<ReactElement>) = createElement(import "DropdownMenuSubContent" "@/components/ui/dropdown-menu") [ prop.children (children :> ReactElement seq) ]
        static member inline dropdownMenuSubContent (text: string) = createElement(import "DropdownMenuSubContent" "@/components/ui/dropdown-menu") [ prop.text text ]

        static member inline form (props: IReactProperty seq) = createElement(import "Form" "@/components/ui/form") props
        static member inline form (children: #seq<ReactElement>) = createElement(import "Form" "@/components/ui/form") [ prop.children (children :> ReactElement seq) ]

        static member inline hoverCard (props: IReactProperty seq) = createElement(import "HoverCard" "@/components/ui/hover-card") props
        static member inline hoverCard (children: #seq<ReactElement>) = createElement(import "HoverCard" "@/components/ui/hover-card") [ prop.children (children :> ReactElement seq) ]
        static member inline hoverCardTrigger (props: IReactProperty seq) = createElement(import "HoverCardTrigger" "@/components/ui/hover-card") props
        static member inline hoverCardTrigger (children: #seq<ReactElement>) = createElement(import "HoverCardTrigger" "@/components/ui/hover-card") [ prop.children (children :> ReactElement seq) ]
        static member inline hoverCardTrigger (text: string) = createElement(import "HoverCardTrigger" "@/components/ui/hover-card") [ prop.text text ]
        static member inline hoverCardContent (props: IReactProperty seq) = createElement(import "HoverCardContent" "@/components/ui/hover-card") props
        static member inline hoverCardContent (children: #seq<ReactElement>) = createElement(import "HoverCardContent" "@/components/ui/hover-card") [ prop.children (children :> ReactElement seq) ]
        static member inline hoverCardContent (text: string) = createElement(import "HoverCardContent" "@/components/ui/hover-card") [ prop.text text ]

        static member inline input (props: IReactProperty seq) = createElement(import "Input" "@/components/ui/input") props
        static member inline input (children: #seq<ReactElement>) = createElement(import "Input" "@/components/ui/input") [ prop.children (children :> ReactElement seq) ]

        static member inline inputOTP (props: IReactProperty seq) = createElement(import "InputOTP" "@/components/ui/input-otp") props
        static member inline inputOTP (children: #seq<ReactElement>) = createElement(import "InputOTP" "@/components/ui/input-otp") [ prop.children (children :> ReactElement seq) ]
        static member inline inputOTPGroup (props: IReactProperty seq) = createElement(import "InputOTPGroup" "@/components/ui/input-otp") props
        static member inline inputOTPGroup (children: #seq<ReactElement>) = createElement(import "InputOTPGroup" "@/components/ui/input-otp") [ prop.children (children :> ReactElement seq) ]
        static member inline inputOTPGroup (text: string) = createElement(import "InputOTPGroup" "@/components/ui/input-otp") [ prop.text text ]
        static member inline inputOTPSlot (props: IReactProperty seq) = createElement(import "InputOTPSlot" "@/components/ui/input-otp") props
        static member inline inputOTPSlot (children: #seq<ReactElement>) = createElement(import "InputOTPSlot" "@/components/ui/input-otp") [ prop.children (children :> ReactElement seq) ]
        static member inline inputOTPSlot (text: string) = createElement(import "InputOTPSlot" "@/components/ui/input-otp") [ prop.text text ]
        static member inline inputOTPSeparator (props: IReactProperty seq) = createElement(import "InputOTPSeparator" "@/components/ui/input-otp") props
        static member inline inputOTPSeparator (children: #seq<ReactElement>) = createElement(import "InputOTPSeparator" "@/components/ui/input-otp") [ prop.children (children :> ReactElement seq) ]
        static member inline inputOTPSeparator (text: string) = createElement(import "InputOTPSeparator" "@/components/ui/input-otp") [ prop.text text ]

        static member inline label (props: IReactProperty seq) = createElement(import "Label" "@/components/ui/label") props
        static member inline label (children: #seq<ReactElement>) = createElement(import "Label" "@/components/ui/label") [ prop.children (children :> ReactElement seq) ]

        static member inline menubar (props: IReactProperty seq) = createElement(import "Menubar" "@/components/ui/menubar") props
        static member inline menubar (children: #seq<ReactElement>) = createElement(import "Menubar" "@/components/ui/menubar") [ prop.children (children :> ReactElement seq) ]
        static member inline menubarPortal (props: IReactProperty seq) = createElement(import "MenubarPortal" "@/components/ui/menubar") props
        static member inline menubarPortal (children: #seq<ReactElement>) = createElement(import "MenubarPortal" "@/components/ui/menubar") [ prop.children (children :> ReactElement seq) ]
        static member inline menubarPortal (text: string) = createElement(import "MenubarPortal" "@/components/ui/menubar") [ prop.text text ]
        static member inline menubarMenu (props: IReactProperty seq) = createElement(import "MenubarMenu" "@/components/ui/menubar") props
        static member inline menubarMenu (children: #seq<ReactElement>) = createElement(import "MenubarMenu" "@/components/ui/menubar") [ prop.children (children :> ReactElement seq) ]
        static member inline menubarMenu (text: string) = createElement(import "MenubarMenu" "@/components/ui/menubar") [ prop.text text ]
        static member inline menubarTrigger (props: IReactProperty seq) = createElement(import "MenubarTrigger" "@/components/ui/menubar") props
        static member inline menubarTrigger (children: #seq<ReactElement>) = createElement(import "MenubarTrigger" "@/components/ui/menubar") [ prop.children (children :> ReactElement seq) ]
        static member inline menubarTrigger (text: string) = createElement(import "MenubarTrigger" "@/components/ui/menubar") [ prop.text text ]
        static member inline menubarContent (props: IReactProperty seq) = createElement(import "MenubarContent" "@/components/ui/menubar") props
        static member inline menubarContent (children: #seq<ReactElement>) = createElement(import "MenubarContent" "@/components/ui/menubar") [ prop.children (children :> ReactElement seq) ]
        static member inline menubarContent (text: string) = createElement(import "MenubarContent" "@/components/ui/menubar") [ prop.text text ]
        static member inline menubarGroup (props: IReactProperty seq) = createElement(import "MenubarGroup" "@/components/ui/menubar") props
        static member inline menubarGroup (children: #seq<ReactElement>) = createElement(import "MenubarGroup" "@/components/ui/menubar") [ prop.children (children :> ReactElement seq) ]
        static member inline menubarGroup (text: string) = createElement(import "MenubarGroup" "@/components/ui/menubar") [ prop.text text ]
        static member inline menubarSeparator (props: IReactProperty seq) = createElement(import "MenubarSeparator" "@/components/ui/menubar") props
        static member inline menubarSeparator (children: #seq<ReactElement>) = createElement(import "MenubarSeparator" "@/components/ui/menubar") [ prop.children (children :> ReactElement seq) ]
        static member inline menubarSeparator (text: string) = createElement(import "MenubarSeparator" "@/components/ui/menubar") [ prop.text text ]
        static member inline menubarLabel (props: IReactProperty seq) = createElement(import "MenubarLabel" "@/components/ui/menubar") props
        static member inline menubarLabel (children: #seq<ReactElement>) = createElement(import "MenubarLabel" "@/components/ui/menubar") [ prop.children (children :> ReactElement seq) ]
        static member inline menubarLabel (text: string) = createElement(import "MenubarLabel" "@/components/ui/menubar") [ prop.text text ]
        static member inline menubarItem (props: IReactProperty seq) = createElement(import "MenubarItem" "@/components/ui/menubar") props
        static member inline menubarItem (children: #seq<ReactElement>) = createElement(import "MenubarItem" "@/components/ui/menubar") [ prop.children (children :> ReactElement seq) ]
        static member inline menubarItem (text: string) = createElement(import "MenubarItem" "@/components/ui/menubar") [ prop.text text ]
        static member inline menubarShortcut (props: IReactProperty seq) = createElement(import "MenubarShortcut" "@/components/ui/menubar") props
        static member inline menubarShortcut (children: #seq<ReactElement>) = createElement(import "MenubarShortcut" "@/components/ui/menubar") [ prop.children (children :> ReactElement seq) ]
        static member inline menubarShortcut (text: string) = createElement(import "MenubarShortcut" "@/components/ui/menubar") [ prop.text text ]
        static member inline menubarCheckboxItem (props: IReactProperty seq) = createElement(import "MenubarCheckboxItem" "@/components/ui/menubar") props
        static member inline menubarCheckboxItem (children: #seq<ReactElement>) = createElement(import "MenubarCheckboxItem" "@/components/ui/menubar") [ prop.children (children :> ReactElement seq) ]
        static member inline menubarCheckboxItem (text: string) = createElement(import "MenubarCheckboxItem" "@/components/ui/menubar") [ prop.text text ]
        static member inline menubarRadioGroup (props: IReactProperty seq) = createElement(import "MenubarRadioGroup" "@/components/ui/menubar") props
        static member inline menubarRadioGroup (children: #seq<ReactElement>) = createElement(import "MenubarRadioGroup" "@/components/ui/menubar") [ prop.children (children :> ReactElement seq) ]
        static member inline menubarRadioGroup (text: string) = createElement(import "MenubarRadioGroup" "@/components/ui/menubar") [ prop.text text ]
        static member inline menubarRadioItem (props: IReactProperty seq) = createElement(import "MenubarRadioItem" "@/components/ui/menubar") props
        static member inline menubarRadioItem (children: #seq<ReactElement>) = createElement(import "MenubarRadioItem" "@/components/ui/menubar") [ prop.children (children :> ReactElement seq) ]
        static member inline menubarRadioItem (text: string) = createElement(import "MenubarRadioItem" "@/components/ui/menubar") [ prop.text text ]
        static member inline menubarSub (props: IReactProperty seq) = createElement(import "MenubarSub" "@/components/ui/menubar") props
        static member inline menubarSub (children: #seq<ReactElement>) = createElement(import "MenubarSub" "@/components/ui/menubar") [ prop.children (children :> ReactElement seq) ]
        static member inline menubarSub (text: string) = createElement(import "MenubarSub" "@/components/ui/menubar") [ prop.text text ]
        static member inline menubarSubTrigger (props: IReactProperty seq) = createElement(import "MenubarSubTrigger" "@/components/ui/menubar") props
        static member inline menubarSubTrigger (children: #seq<ReactElement>) = createElement(import "MenubarSubTrigger" "@/components/ui/menubar") [ prop.children (children :> ReactElement seq) ]
        static member inline menubarSubTrigger (text: string) = createElement(import "MenubarSubTrigger" "@/components/ui/menubar") [ prop.text text ]
        static member inline menubarSubContent (props: IReactProperty seq) = createElement(import "MenubarSubContent" "@/components/ui/menubar") props
        static member inline menubarSubContent (children: #seq<ReactElement>) = createElement(import "MenubarSubContent" "@/components/ui/menubar") [ prop.children (children :> ReactElement seq) ]
        static member inline menubarSubContent (text: string) = createElement(import "MenubarSubContent" "@/components/ui/menubar") [ prop.text text ]

        static member inline navigationMenu (props: IReactProperty seq) = createElement(import "NavigationMenu" "@/components/ui/navigation-menu") props
        static member inline navigationMenu (children: #seq<ReactElement>) = createElement(import "NavigationMenu" "@/components/ui/navigation-menu") [ prop.children (children :> ReactElement seq) ]
        static member inline navigationMenuList (props: IReactProperty seq) = createElement(import "NavigationMenuList" "@/components/ui/navigation-menu") props
        static member inline navigationMenuList (children: #seq<ReactElement>) = createElement(import "NavigationMenuList" "@/components/ui/navigation-menu") [ prop.children (children :> ReactElement seq) ]
        static member inline navigationMenuList (text: string) = createElement(import "NavigationMenuList" "@/components/ui/navigation-menu") [ prop.text text ]
        static member inline navigationMenuItem (props: IReactProperty seq) = createElement(import "NavigationMenuItem" "@/components/ui/navigation-menu") props
        static member inline navigationMenuItem (children: #seq<ReactElement>) = createElement(import "NavigationMenuItem" "@/components/ui/navigation-menu") [ prop.children (children :> ReactElement seq) ]
        static member inline navigationMenuItem (text: string) = createElement(import "NavigationMenuItem" "@/components/ui/navigation-menu") [ prop.text text ]
        static member inline navigationMenuContent (props: IReactProperty seq) = createElement(import "NavigationMenuContent" "@/components/ui/navigation-menu") props
        static member inline navigationMenuContent (children: #seq<ReactElement>) = createElement(import "NavigationMenuContent" "@/components/ui/navigation-menu") [ prop.children (children :> ReactElement seq) ]
        static member inline navigationMenuContent (text: string) = createElement(import "NavigationMenuContent" "@/components/ui/navigation-menu") [ prop.text text ]
        static member inline navigationMenuTrigger (props: IReactProperty seq) = createElement(import "NavigationMenuTrigger" "@/components/ui/navigation-menu") props
        static member inline navigationMenuTrigger (children: #seq<ReactElement>) = createElement(import "NavigationMenuTrigger" "@/components/ui/navigation-menu") [ prop.children (children :> ReactElement seq) ]
        static member inline navigationMenuTrigger (text: string) = createElement(import "NavigationMenuTrigger" "@/components/ui/navigation-menu") [ prop.text text ]
        static member inline navigationMenuLink (props: IReactProperty seq) = createElement(import "NavigationMenuLink" "@/components/ui/navigation-menu") props
        static member inline navigationMenuLink (children: #seq<ReactElement>) = createElement(import "NavigationMenuLink" "@/components/ui/navigation-menu") [ prop.children (children :> ReactElement seq) ]
        static member inline navigationMenuLink (text: string) = createElement(import "NavigationMenuLink" "@/components/ui/navigation-menu") [ prop.text text ]
        static member inline navigationMenuIndicator (props: IReactProperty seq) = createElement(import "NavigationMenuIndicator" "@/components/ui/navigation-menu") props
        static member inline navigationMenuIndicator (children: #seq<ReactElement>) = createElement(import "NavigationMenuIndicator" "@/components/ui/navigation-menu") [ prop.children (children :> ReactElement seq) ]
        static member inline navigationMenuIndicator (text: string) = createElement(import "NavigationMenuIndicator" "@/components/ui/navigation-menu") [ prop.text text ]
        static member inline navigationMenuViewport (props: IReactProperty seq) = createElement(import "NavigationMenuViewport" "@/components/ui/navigation-menu") props
        static member inline navigationMenuViewport (children: #seq<ReactElement>) = createElement(import "NavigationMenuViewport" "@/components/ui/navigation-menu") [ prop.children (children :> ReactElement seq) ]
        static member inline navigationMenuViewport (text: string) = createElement(import "NavigationMenuViewport" "@/components/ui/navigation-menu") [ prop.text text ]

        static member inline pagination (props: IReactProperty seq) = createElement(import "Pagination" "@/components/ui/pagination") props
        static member inline pagination (children: #seq<ReactElement>) = createElement(import "Pagination" "@/components/ui/pagination") [ prop.children (children :> ReactElement seq) ]
        static member inline paginationContent (props: IReactProperty seq) = createElement(import "PaginationContent" "@/components/ui/pagination") props
        static member inline paginationContent (children: #seq<ReactElement>) = createElement(import "PaginationContent" "@/components/ui/pagination") [ prop.children (children :> ReactElement seq) ]
        static member inline paginationContent (text: string) = createElement(import "PaginationContent" "@/components/ui/pagination") [ prop.text text ]
        static member inline paginationLink (props: IReactProperty seq) = createElement(import "PaginationLink" "@/components/ui/pagination") props
        static member inline paginationLink (children: #seq<ReactElement>) = createElement(import "PaginationLink" "@/components/ui/pagination") [ prop.children (children :> ReactElement seq) ]
        static member inline paginationLink (text: string) = createElement(import "PaginationLink" "@/components/ui/pagination") [ prop.text text ]
        static member inline paginationItem (props: IReactProperty seq) = createElement(import "PaginationItem" "@/components/ui/pagination") props
        static member inline paginationItem (children: #seq<ReactElement>) = createElement(import "PaginationItem" "@/components/ui/pagination") [ prop.children (children :> ReactElement seq) ]
        static member inline paginationItem (text: string) = createElement(import "PaginationItem" "@/components/ui/pagination") [ prop.text text ]
        static member inline paginationPrevious (props: IReactProperty seq) = createElement(import "PaginationPrevious" "@/components/ui/pagination") props
        static member inline paginationPrevious (children: #seq<ReactElement>) = createElement(import "PaginationPrevious" "@/components/ui/pagination") [ prop.children (children :> ReactElement seq) ]
        static member inline paginationPrevious (text: string) = createElement(import "PaginationPrevious" "@/components/ui/pagination") [ prop.text text ]
        static member inline paginationNext (props: IReactProperty seq) = createElement(import "PaginationNext" "@/components/ui/pagination") props
        static member inline paginationNext (children: #seq<ReactElement>) = createElement(import "PaginationNext" "@/components/ui/pagination") [ prop.children (children :> ReactElement seq) ]
        static member inline paginationNext (text: string) = createElement(import "PaginationNext" "@/components/ui/pagination") [ prop.text text ]
        static member inline paginationEllipsis (props: IReactProperty seq) = createElement(import "PaginationEllipsis" "@/components/ui/pagination") props
        static member inline paginationEllipsis (children: #seq<ReactElement>) = createElement(import "PaginationEllipsis" "@/components/ui/pagination") [ prop.children (children :> ReactElement seq) ]
        static member inline paginationEllipsis (text: string) = createElement(import "PaginationEllipsis" "@/components/ui/pagination") [ prop.text text ]

        static member inline popover (props: IReactProperty seq) = createElement(import "Popover" "@/components/ui/popover") props
        static member inline popover (children: #seq<ReactElement>) = createElement(import "Popover" "@/components/ui/popover") [ prop.children (children :> ReactElement seq) ]
        static member inline popoverTrigger (props: IReactProperty seq) = createElement(import "PopoverTrigger" "@/components/ui/popover") props
        static member inline popoverTrigger (children: #seq<ReactElement>) = createElement(import "PopoverTrigger" "@/components/ui/popover") [ prop.children (children :> ReactElement seq) ]
        static member inline popoverTrigger (text: string) = createElement(import "PopoverTrigger" "@/components/ui/popover") [ prop.text text ]
        static member inline popoverContent (props: IReactProperty seq) = createElement(import "PopoverContent" "@/components/ui/popover") props
        static member inline popoverContent (children: #seq<ReactElement>) = createElement(import "PopoverContent" "@/components/ui/popover") [ prop.children (children :> ReactElement seq) ]
        static member inline popoverContent (text: string) = createElement(import "PopoverContent" "@/components/ui/popover") [ prop.text text ]
        static member inline popoverAnchor (props: IReactProperty seq) = createElement(import "PopoverAnchor" "@/components/ui/popover") props
        static member inline popoverAnchor (children: #seq<ReactElement>) = createElement(import "PopoverAnchor" "@/components/ui/popover") [ prop.children (children :> ReactElement seq) ]
        static member inline popoverAnchor (text: string) = createElement(import "PopoverAnchor" "@/components/ui/popover") [ prop.text text ]

        static member inline progress (props: IReactProperty seq) = createElement(import "Progress" "@/components/ui/progress") props
        static member inline progress (children: #seq<ReactElement>) = createElement(import "Progress" "@/components/ui/progress") [ prop.children (children :> ReactElement seq) ]

        static member inline radioGroup (props: IReactProperty seq) = createElement(import "RadioGroup" "@/components/ui/radio-group") props
        static member inline radioGroup (children: #seq<ReactElement>) = createElement(import "RadioGroup" "@/components/ui/radio-group") [ prop.children (children :> ReactElement seq) ]
        static member inline radioGroupItem (props: IReactProperty seq) = createElement(import "RadioGroupItem" "@/components/ui/radio-group") props
        static member inline radioGroupItem (children: #seq<ReactElement>) = createElement(import "RadioGroupItem" "@/components/ui/radio-group") [ prop.children (children :> ReactElement seq) ]
        static member inline radioGroupItem (text: string) = createElement(import "RadioGroupItem" "@/components/ui/radio-group") [ prop.text text ]

        static member inline resizablePanelGroup (props: IReactProperty seq) = createElement(import "ResizablePanelGroup" "@/components/ui/resizable") props
        static member inline resizablePanelGroup (children: #seq<ReactElement>) = createElement(import "ResizablePanelGroup" "@/components/ui/resizable") [ prop.children (children :> ReactElement seq) ]

        static member inline scrollArea (props: IReactProperty seq) = createElement(import "ScrollArea" "@/components/ui/scroll-area") props
        static member inline scrollArea (children: #seq<ReactElement>) = createElement(import "ScrollArea" "@/components/ui/scroll-area") [ prop.children (children :> ReactElement seq) ]

        static member inline select (props: IReactProperty seq) = createElement(import "Select" "@/components/ui/select") props
        static member inline select (children: #seq<ReactElement>) = createElement(import "Select" "@/components/ui/select") [ prop.children (children :> ReactElement seq) ]
        static member inline selectContent (props: IReactProperty seq) = createElement(import "SelectContent" "@/components/ui/select") props
        static member inline selectContent (children: #seq<ReactElement>) = createElement(import "SelectContent" "@/components/ui/select") [ prop.children (children :> ReactElement seq) ]
        static member inline selectContent (text: string) = createElement(import "SelectContent" "@/components/ui/select") [ prop.text text ]
        static member inline selectGroup (props: IReactProperty seq) = createElement(import "SelectGroup" "@/components/ui/select") props
        static member inline selectGroup (children: #seq<ReactElement>) = createElement(import "SelectGroup" "@/components/ui/select") [ prop.children (children :> ReactElement seq) ]
        static member inline selectGroup (text: string) = createElement(import "SelectGroup" "@/components/ui/select") [ prop.text text ]
        static member inline selectItem (props: IReactProperty seq) = createElement(import "SelectItem" "@/components/ui/select") props
        static member inline selectItem (children: #seq<ReactElement>) = createElement(import "SelectItem" "@/components/ui/select") [ prop.children (children :> ReactElement seq) ]
        static member inline selectItem (text: string) = createElement(import "SelectItem" "@/components/ui/select") [ prop.text text ]
        static member inline selectLabel (props: IReactProperty seq) = createElement(import "SelectLabel" "@/components/ui/select") props
        static member inline selectLabel (children: #seq<ReactElement>) = createElement(import "SelectLabel" "@/components/ui/select") [ prop.children (children :> ReactElement seq) ]
        static member inline selectLabel (text: string) = createElement(import "SelectLabel" "@/components/ui/select") [ prop.text text ]
        static member inline selectScrollDownButton (props: IReactProperty seq) = createElement(import "SelectScrollDownButton" "@/components/ui/select") props
        static member inline selectScrollDownButton (children: #seq<ReactElement>) = createElement(import "SelectScrollDownButton" "@/components/ui/select") [ prop.children (children :> ReactElement seq) ]
        static member inline selectScrollDownButton (text: string) = createElement(import "SelectScrollDownButton" "@/components/ui/select") [ prop.text text ]
        static member inline selectScrollUpButton (props: IReactProperty seq) = createElement(import "SelectScrollUpButton" "@/components/ui/select") props
        static member inline selectScrollUpButton (children: #seq<ReactElement>) = createElement(import "SelectScrollUpButton" "@/components/ui/select") [ prop.children (children :> ReactElement seq) ]
        static member inline selectScrollUpButton (text: string) = createElement(import "SelectScrollUpButton" "@/components/ui/select") [ prop.text text ]
        static member inline selectSeparator (props: IReactProperty seq) = createElement(import "SelectSeparator" "@/components/ui/select") props
        static member inline selectSeparator (children: #seq<ReactElement>) = createElement(import "SelectSeparator" "@/components/ui/select") [ prop.children (children :> ReactElement seq) ]
        static member inline selectSeparator (text: string) = createElement(import "SelectSeparator" "@/components/ui/select") [ prop.text text ]
        static member inline selectTrigger (props: IReactProperty seq) = createElement(import "SelectTrigger" "@/components/ui/select") props
        static member inline selectTrigger (children: #seq<ReactElement>) = createElement(import "SelectTrigger" "@/components/ui/select") [ prop.children (children :> ReactElement seq) ]
        static member inline selectTrigger (text: string) = createElement(import "SelectTrigger" "@/components/ui/select") [ prop.text text ]
        static member inline selectValue (props: IReactProperty seq) = createElement(import "SelectValue" "@/components/ui/select") props
        static member inline selectValue (children: #seq<ReactElement>) = createElement(import "SelectValue" "@/components/ui/select") [ prop.children (children :> ReactElement seq) ]
        static member inline selectValue (text: string) = createElement(import "SelectValue" "@/components/ui/select") [ prop.text text ]

        static member inline separator (props: IReactProperty seq) = createElement(import "Separator" "@/components/ui/separator") props
        static member inline separator (children: #seq<ReactElement>) = createElement(import "Separator" "@/components/ui/separator") [ prop.children (children :> ReactElement seq) ]

        static member inline sheet (props: IReactProperty seq) = createElement(import "Sheet" "@/components/ui/sheet") props
        static member inline sheet (children: #seq<ReactElement>) = createElement(import "Sheet" "@/components/ui/sheet") [ prop.children (children :> ReactElement seq) ]
        static member inline sheetTrigger (props: IReactProperty seq) = createElement(import "SheetTrigger" "@/components/ui/sheet") props
        static member inline sheetTrigger (children: #seq<ReactElement>) = createElement(import "SheetTrigger" "@/components/ui/sheet") [ prop.children (children :> ReactElement seq) ]
        static member inline sheetTrigger (text: string) = createElement(import "SheetTrigger" "@/components/ui/sheet") [ prop.text text ]
        static member inline sheetClose (props: IReactProperty seq) = createElement(import "SheetClose" "@/components/ui/sheet") props
        static member inline sheetClose (children: #seq<ReactElement>) = createElement(import "SheetClose" "@/components/ui/sheet") [ prop.children (children :> ReactElement seq) ]
        static member inline sheetClose (text: string) = createElement(import "SheetClose" "@/components/ui/sheet") [ prop.text text ]
        static member inline sheetContent (props: IReactProperty seq) = createElement(import "SheetContent" "@/components/ui/sheet") props
        static member inline sheetContent (children: #seq<ReactElement>) = createElement(import "SheetContent" "@/components/ui/sheet") [ prop.children (children :> ReactElement seq) ]
        static member inline sheetContent (text: string) = createElement(import "SheetContent" "@/components/ui/sheet") [ prop.text text ]
        static member inline sheetHeader (props: IReactProperty seq) = createElement(import "SheetHeader" "@/components/ui/sheet") props
        static member inline sheetHeader (children: #seq<ReactElement>) = createElement(import "SheetHeader" "@/components/ui/sheet") [ prop.children (children :> ReactElement seq) ]
        static member inline sheetHeader (text: string) = createElement(import "SheetHeader" "@/components/ui/sheet") [ prop.text text ]
        static member inline sheetFooter (props: IReactProperty seq) = createElement(import "SheetFooter" "@/components/ui/sheet") props
        static member inline sheetFooter (children: #seq<ReactElement>) = createElement(import "SheetFooter" "@/components/ui/sheet") [ prop.children (children :> ReactElement seq) ]
        static member inline sheetFooter (text: string) = createElement(import "SheetFooter" "@/components/ui/sheet") [ prop.text text ]
        static member inline sheetTitle (props: IReactProperty seq) = createElement(import "SheetTitle" "@/components/ui/sheet") props
        static member inline sheetTitle (children: #seq<ReactElement>) = createElement(import "SheetTitle" "@/components/ui/sheet") [ prop.children (children :> ReactElement seq) ]
        static member inline sheetTitle (text: string) = createElement(import "SheetTitle" "@/components/ui/sheet") [ prop.text text ]
        static member inline sheetDescription (props: IReactProperty seq) = createElement(import "SheetDescription" "@/components/ui/sheet") props
        static member inline sheetDescription (children: #seq<ReactElement>) = createElement(import "SheetDescription" "@/components/ui/sheet") [ prop.children (children :> ReactElement seq) ]
        static member inline sheetDescription (text: string) = createElement(import "SheetDescription" "@/components/ui/sheet") [ prop.text text ]

        static member inline sidebar (props: IReactProperty seq) = createElement(import "Sidebar" "@/components/ui/sidebar") props
        static member inline sidebar (children: #seq<ReactElement>) = createElement(import "Sidebar" "@/components/ui/sidebar") [ prop.children (children :> ReactElement seq) ]
        static member inline sidebarContent (props: IReactProperty seq) = createElement(import "SidebarContent" "@/components/ui/sidebar") props
        static member inline sidebarContent (children: #seq<ReactElement>) = createElement(import "SidebarContent" "@/components/ui/sidebar") [ prop.children (children :> ReactElement seq) ]
        static member inline sidebarContent (text: string) = createElement(import "SidebarContent" "@/components/ui/sidebar") [ prop.text text ]
        static member inline sidebarFooter (props: IReactProperty seq) = createElement(import "SidebarFooter" "@/components/ui/sidebar") props
        static member inline sidebarFooter (children: #seq<ReactElement>) = createElement(import "SidebarFooter" "@/components/ui/sidebar") [ prop.children (children :> ReactElement seq) ]
        static member inline sidebarFooter (text: string) = createElement(import "SidebarFooter" "@/components/ui/sidebar") [ prop.text text ]
        static member inline sidebarGroup (props: IReactProperty seq) = createElement(import "SidebarGroup" "@/components/ui/sidebar") props
        static member inline sidebarGroup (children: #seq<ReactElement>) = createElement(import "SidebarGroup" "@/components/ui/sidebar") [ prop.children (children :> ReactElement seq) ]
        static member inline sidebarGroup (text: string) = createElement(import "SidebarGroup" "@/components/ui/sidebar") [ prop.text text ]
        static member inline sidebarGroupAction (props: IReactProperty seq) = createElement(import "SidebarGroupAction" "@/components/ui/sidebar") props
        static member inline sidebarGroupAction (children: #seq<ReactElement>) = createElement(import "SidebarGroupAction" "@/components/ui/sidebar") [ prop.children (children :> ReactElement seq) ]
        static member inline sidebarGroupAction (text: string) = createElement(import "SidebarGroupAction" "@/components/ui/sidebar") [ prop.text text ]
        static member inline sidebarGroupContent (props: IReactProperty seq) = createElement(import "SidebarGroupContent" "@/components/ui/sidebar") props
        static member inline sidebarGroupContent (children: #seq<ReactElement>) = createElement(import "SidebarGroupContent" "@/components/ui/sidebar") [ prop.children (children :> ReactElement seq) ]
        static member inline sidebarGroupContent (text: string) = createElement(import "SidebarGroupContent" "@/components/ui/sidebar") [ prop.text text ]
        static member inline sidebarGroupLabel (props: IReactProperty seq) = createElement(import "SidebarGroupLabel" "@/components/ui/sidebar") props
        static member inline sidebarGroupLabel (children: #seq<ReactElement>) = createElement(import "SidebarGroupLabel" "@/components/ui/sidebar") [ prop.children (children :> ReactElement seq) ]
        static member inline sidebarGroupLabel (text: string) = createElement(import "SidebarGroupLabel" "@/components/ui/sidebar") [ prop.text text ]
        static member inline sidebarHeader (props: IReactProperty seq) = createElement(import "SidebarHeader" "@/components/ui/sidebar") props
        static member inline sidebarHeader (children: #seq<ReactElement>) = createElement(import "SidebarHeader" "@/components/ui/sidebar") [ prop.children (children :> ReactElement seq) ]
        static member inline sidebarHeader (text: string) = createElement(import "SidebarHeader" "@/components/ui/sidebar") [ prop.text text ]
        static member inline sidebarInput (props: IReactProperty seq) = createElement(import "SidebarInput" "@/components/ui/sidebar") props
        static member inline sidebarInput (children: #seq<ReactElement>) = createElement(import "SidebarInput" "@/components/ui/sidebar") [ prop.children (children :> ReactElement seq) ]
        static member inline sidebarInput (text: string) = createElement(import "SidebarInput" "@/components/ui/sidebar") [ prop.text text ]
        static member inline sidebarInset (props: IReactProperty seq) = createElement(import "SidebarInset" "@/components/ui/sidebar") props
        static member inline sidebarInset (children: #seq<ReactElement>) = createElement(import "SidebarInset" "@/components/ui/sidebar") [ prop.children (children :> ReactElement seq) ]
        static member inline sidebarInset (text: string) = createElement(import "SidebarInset" "@/components/ui/sidebar") [ prop.text text ]
        static member inline sidebarMenu (props: IReactProperty seq) = createElement(import "SidebarMenu" "@/components/ui/sidebar") props
        static member inline sidebarMenu (children: #seq<ReactElement>) = createElement(import "SidebarMenu" "@/components/ui/sidebar") [ prop.children (children :> ReactElement seq) ]
        static member inline sidebarMenu (text: string) = createElement(import "SidebarMenu" "@/components/ui/sidebar") [ prop.text text ]
        static member inline sidebarMenuAction (props: IReactProperty seq) = createElement(import "SidebarMenuAction" "@/components/ui/sidebar") props
        static member inline sidebarMenuAction (children: #seq<ReactElement>) = createElement(import "SidebarMenuAction" "@/components/ui/sidebar") [ prop.children (children :> ReactElement seq) ]
        static member inline sidebarMenuAction (text: string) = createElement(import "SidebarMenuAction" "@/components/ui/sidebar") [ prop.text text ]
        static member inline sidebarMenuBadge (props: IReactProperty seq) = createElement(import "SidebarMenuBadge" "@/components/ui/sidebar") props
        static member inline sidebarMenuBadge (children: #seq<ReactElement>) = createElement(import "SidebarMenuBadge" "@/components/ui/sidebar") [ prop.children (children :> ReactElement seq) ]
        static member inline sidebarMenuBadge (text: string) = createElement(import "SidebarMenuBadge" "@/components/ui/sidebar") [ prop.text text ]
        static member inline sidebarMenuButton (props: IReactProperty seq) = createElement(import "SidebarMenuButton" "@/components/ui/sidebar") props
        static member inline sidebarMenuButton (children: #seq<ReactElement>) = createElement(import "SidebarMenuButton" "@/components/ui/sidebar") [ prop.children (children :> ReactElement seq) ]
        static member inline sidebarMenuButton (text: string) = createElement(import "SidebarMenuButton" "@/components/ui/sidebar") [ prop.text text ]
        static member inline sidebarMenuItem (props: IReactProperty seq) = createElement(import "SidebarMenuItem" "@/components/ui/sidebar") props
        static member inline sidebarMenuItem (children: #seq<ReactElement>) = createElement(import "SidebarMenuItem" "@/components/ui/sidebar") [ prop.children (children :> ReactElement seq) ]
        static member inline sidebarMenuItem (text: string) = createElement(import "SidebarMenuItem" "@/components/ui/sidebar") [ prop.text text ]
        static member inline sidebarMenuSkeleton (props: IReactProperty seq) = createElement(import "SidebarMenuSkeleton" "@/components/ui/sidebar") props
        static member inline sidebarMenuSkeleton (children: #seq<ReactElement>) = createElement(import "SidebarMenuSkeleton" "@/components/ui/sidebar") [ prop.children (children :> ReactElement seq) ]
        static member inline sidebarMenuSkeleton (text: string) = createElement(import "SidebarMenuSkeleton" "@/components/ui/sidebar") [ prop.text text ]
        static member inline sidebarMenuSub (props: IReactProperty seq) = createElement(import "SidebarMenuSub" "@/components/ui/sidebar") props
        static member inline sidebarMenuSub (children: #seq<ReactElement>) = createElement(import "SidebarMenuSub" "@/components/ui/sidebar") [ prop.children (children :> ReactElement seq) ]
        static member inline sidebarMenuSub (text: string) = createElement(import "SidebarMenuSub" "@/components/ui/sidebar") [ prop.text text ]
        static member inline sidebarMenuSubButton (props: IReactProperty seq) = createElement(import "SidebarMenuSubButton" "@/components/ui/sidebar") props
        static member inline sidebarMenuSubButton (children: #seq<ReactElement>) = createElement(import "SidebarMenuSubButton" "@/components/ui/sidebar") [ prop.children (children :> ReactElement seq) ]
        static member inline sidebarMenuSubButton (text: string) = createElement(import "SidebarMenuSubButton" "@/components/ui/sidebar") [ prop.text text ]
        static member inline sidebarMenuSubItem (props: IReactProperty seq) = createElement(import "SidebarMenuSubItem" "@/components/ui/sidebar") props
        static member inline sidebarMenuSubItem (children: #seq<ReactElement>) = createElement(import "SidebarMenuSubItem" "@/components/ui/sidebar") [ prop.children (children :> ReactElement seq) ]
        static member inline sidebarMenuSubItem (text: string) = createElement(import "SidebarMenuSubItem" "@/components/ui/sidebar") [ prop.text text ]
        static member inline sidebarProvider (props: IReactProperty seq) = createElement(import "SidebarProvider" "@/components/ui/sidebar") props
        static member inline sidebarProvider (children: #seq<ReactElement>) = createElement(import "SidebarProvider" "@/components/ui/sidebar") [ prop.children (children :> ReactElement seq) ]
        static member inline sidebarProvider (text: string) = createElement(import "SidebarProvider" "@/components/ui/sidebar") [ prop.text text ]
        static member inline sidebarRail (props: IReactProperty seq) = createElement(import "SidebarRail" "@/components/ui/sidebar") props
        static member inline sidebarRail (children: #seq<ReactElement>) = createElement(import "SidebarRail" "@/components/ui/sidebar") [ prop.children (children :> ReactElement seq) ]
        static member inline sidebarRail (text: string) = createElement(import "SidebarRail" "@/components/ui/sidebar") [ prop.text text ]
        static member inline sidebarSeparator (props: IReactProperty seq) = createElement(import "SidebarSeparator" "@/components/ui/sidebar") props
        static member inline sidebarSeparator (children: #seq<ReactElement>) = createElement(import "SidebarSeparator" "@/components/ui/sidebar") [ prop.children (children :> ReactElement seq) ]
        static member inline sidebarSeparator (text: string) = createElement(import "SidebarSeparator" "@/components/ui/sidebar") [ prop.text text ]
        static member inline sidebarTrigger (props: IReactProperty seq) = createElement(import "SidebarTrigger" "@/components/ui/sidebar") props
        static member inline sidebarTrigger (children: #seq<ReactElement>) = createElement(import "SidebarTrigger" "@/components/ui/sidebar") [ prop.children (children :> ReactElement seq) ]
        static member inline sidebarTrigger (text: string) = createElement(import "SidebarTrigger" "@/components/ui/sidebar") [ prop.text text ]

        static member inline skeleton (props: IReactProperty seq) = createElement(import "Skeleton" "@/components/ui/skeleton") props
        static member inline skeleton (children: #seq<ReactElement>) = createElement(import "Skeleton" "@/components/ui/skeleton") [ prop.children (children :> ReactElement seq) ]

        static member inline slider (props: IReactProperty seq) = createElement(import "Slider" "@/components/ui/slider") props
        static member inline slider (children: #seq<ReactElement>) = createElement(import "Slider" "@/components/ui/slider") [ prop.children (children :> ReactElement seq) ]

        static member inline toaster (props: IReactProperty seq) = createElement(import "Toaster" "@/components/ui/sonner") props
        static member inline toaster (children: #seq<ReactElement>) = createElement(import "Toaster" "@/components/ui/sonner") [ prop.children (children :> ReactElement seq) ]

        static member inline switch (props: IReactProperty seq) = createElement(import "Switch" "@/components/ui/switch") props
        static member inline switch (children: #seq<ReactElement>) = createElement(import "Switch" "@/components/ui/switch") [ prop.children (children :> ReactElement seq) ]

        static member inline table (props: IReactProperty seq) = createElement(import "Table" "@/components/ui/table") props
        static member inline table (children: #seq<ReactElement>) = createElement(import "Table" "@/components/ui/table") [ prop.children (children :> ReactElement seq) ]
        static member inline tableHeader (props: IReactProperty seq) = createElement(import "TableHeader" "@/components/ui/table") props
        static member inline tableHeader (children: #seq<ReactElement>) = createElement(import "TableHeader" "@/components/ui/table") [ prop.children (children :> ReactElement seq) ]
        static member inline tableHeader (text: string) = createElement(import "TableHeader" "@/components/ui/table") [ prop.text text ]
        static member inline tableBody (props: IReactProperty seq) = createElement(import "TableBody" "@/components/ui/table") props
        static member inline tableBody (children: #seq<ReactElement>) = createElement(import "TableBody" "@/components/ui/table") [ prop.children (children :> ReactElement seq) ]
        static member inline tableBody (text: string) = createElement(import "TableBody" "@/components/ui/table") [ prop.text text ]
        static member inline tableFooter (props: IReactProperty seq) = createElement(import "TableFooter" "@/components/ui/table") props
        static member inline tableFooter (children: #seq<ReactElement>) = createElement(import "TableFooter" "@/components/ui/table") [ prop.children (children :> ReactElement seq) ]
        static member inline tableFooter (text: string) = createElement(import "TableFooter" "@/components/ui/table") [ prop.text text ]
        static member inline tableHead (props: IReactProperty seq) = createElement(import "TableHead" "@/components/ui/table") props
        static member inline tableHead (children: #seq<ReactElement>) = createElement(import "TableHead" "@/components/ui/table") [ prop.children (children :> ReactElement seq) ]
        static member inline tableHead (text: string) = createElement(import "TableHead" "@/components/ui/table") [ prop.text text ]
        static member inline tableRow (props: IReactProperty seq) = createElement(import "TableRow" "@/components/ui/table") props
        static member inline tableRow (children: #seq<ReactElement>) = createElement(import "TableRow" "@/components/ui/table") [ prop.children (children :> ReactElement seq) ]
        static member inline tableRow (text: string) = createElement(import "TableRow" "@/components/ui/table") [ prop.text text ]
        static member inline tableCell (props: IReactProperty seq) = createElement(import "TableCell" "@/components/ui/table") props
        static member inline tableCell (children: #seq<ReactElement>) = createElement(import "TableCell" "@/components/ui/table") [ prop.children (children :> ReactElement seq) ]
        static member inline tableCell (text: string) = createElement(import "TableCell" "@/components/ui/table") [ prop.text text ]
        static member inline tableCaption (props: IReactProperty seq) = createElement(import "TableCaption" "@/components/ui/table") props
        static member inline tableCaption (children: #seq<ReactElement>) = createElement(import "TableCaption" "@/components/ui/table") [ prop.children (children :> ReactElement seq) ]
        static member inline tableCaption (text: string) = createElement(import "TableCaption" "@/components/ui/table") [ prop.text text ]

        static member inline tabs (props: IReactProperty seq) = createElement(import "Tabs" "@/components/ui/tabs") props
        static member inline tabs (children: #seq<ReactElement>) = createElement(import "Tabs" "@/components/ui/tabs") [ prop.children (children :> ReactElement seq) ]
        static member inline tabsList (props: IReactProperty seq) = createElement(import "TabsList" "@/components/ui/tabs") props
        static member inline tabsList (children: #seq<ReactElement>) = createElement(import "TabsList" "@/components/ui/tabs") [ prop.children (children :> ReactElement seq) ]
        static member inline tabsList (text: string) = createElement(import "TabsList" "@/components/ui/tabs") [ prop.text text ]
        static member inline tabsTrigger (props: IReactProperty seq) = createElement(import "TabsTrigger" "@/components/ui/tabs") props
        static member inline tabsTrigger (children: #seq<ReactElement>) = createElement(import "TabsTrigger" "@/components/ui/tabs") [ prop.children (children :> ReactElement seq) ]
        static member inline tabsTrigger (text: string) = createElement(import "TabsTrigger" "@/components/ui/tabs") [ prop.text text ]
        static member inline tabsContent (props: IReactProperty seq) = createElement(import "TabsContent" "@/components/ui/tabs") props
        static member inline tabsContent (children: #seq<ReactElement>) = createElement(import "TabsContent" "@/components/ui/tabs") [ prop.children (children :> ReactElement seq) ]
        static member inline tabsContent (text: string) = createElement(import "TabsContent" "@/components/ui/tabs") [ prop.text text ]

        static member inline textarea (props: IReactProperty seq) = createElement(import "Textarea" "@/components/ui/textarea") props
        static member inline textarea (children: #seq<ReactElement>) = createElement(import "Textarea" "@/components/ui/textarea") [ prop.children (children :> ReactElement seq) ]

        static member inline toggle (props: IReactProperty seq) = createElement(import "Toggle" "@/components/ui/toggle") props
        static member inline toggle (children: #seq<ReactElement>) = createElement(import "Toggle" "@/components/ui/toggle") [ prop.children (children :> ReactElement seq) ]

        static member inline toggleGroup (props: IReactProperty seq) = createElement(import "ToggleGroup" "@/components/ui/toggle-group") props
        static member inline toggleGroup (children: #seq<ReactElement>) = createElement(import "ToggleGroup" "@/components/ui/toggle-group") [ prop.children (children :> ReactElement seq) ]
        static member inline toggleGroupItem (props: IReactProperty seq) = createElement(import "ToggleGroupItem" "@/components/ui/toggle-group") props
        static member inline toggleGroupItem (children: #seq<ReactElement>) = createElement(import "ToggleGroupItem" "@/components/ui/toggle-group") [ prop.children (children :> ReactElement seq) ]
        static member inline toggleGroupItem (text: string) = createElement(import "ToggleGroupItem" "@/components/ui/toggle-group") [ prop.text text ]

        static member inline tooltip (props: IReactProperty seq) = createElement(import "Tooltip" "@/components/ui/tooltip") props
        static member inline tooltip (children: #seq<ReactElement>) = createElement(import "Tooltip" "@/components/ui/tooltip") [ prop.children (children :> ReactElement seq) ]
        static member inline tooltipTrigger (props: IReactProperty seq) = createElement(import "TooltipTrigger" "@/components/ui/tooltip") props
        static member inline tooltipTrigger (children: #seq<ReactElement>) = createElement(import "TooltipTrigger" "@/components/ui/tooltip") [ prop.children (children :> ReactElement seq) ]
        static member inline tooltipTrigger (text: string) = createElement(import "TooltipTrigger" "@/components/ui/tooltip") [ prop.text text ]
        static member inline tooltipContent (props: IReactProperty seq) = createElement(import "TooltipContent" "@/components/ui/tooltip") props
        static member inline tooltipContent (children: #seq<ReactElement>) = createElement(import "TooltipContent" "@/components/ui/tooltip") [ prop.children (children :> ReactElement seq) ]
        static member inline tooltipContent (text: string) = createElement(import "TooltipContent" "@/components/ui/tooltip") [ prop.text text ]
        static member inline tooltipProvider (props: IReactProperty seq) = createElement(import "TooltipProvider" "@/components/ui/tooltip") props
        static member inline tooltipProvider (children: #seq<ReactElement>) = createElement(import "TooltipProvider" "@/components/ui/tooltip") [ prop.children (children :> ReactElement seq) ]
        static member inline tooltipProvider (text: string) = createElement(import "TooltipProvider" "@/components/ui/tooltip") [ prop.text text ]

